## Activate Julia environment

Enter the ````pkg````REPL from the julia ````]```` fromt the Julia REPL. 

````activate Atom_Sim````


If you have the same package (at the same version) installed in multiple environments, the package will only be downloaded and stored on the hard drive once. This makes environments very lightweight and effectively free to create.


Then add packages

````add AtomTwin````